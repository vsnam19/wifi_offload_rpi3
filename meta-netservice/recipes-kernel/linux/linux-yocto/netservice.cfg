# netservice.cfg — kernel config fragment for WiFi Offload Manager
# Applied via linux-yocto bbappend on top of the qemuarm defconfig.
# All options required; bitbake -c kernel_configcheck will verify them.

# ── MPTCP ────────────────────────────────────────────────────────
CONFIG_MPTCP=y
CONFIG_INET_MPTCP_DIAG=y

# ── cgroup net_cls — process-level traffic classification ─────────
CONFIG_CGROUP_NET_CLASSID=y
CONFIG_CGROUP_NET_PRIO=y

# ── netfilter — classid→fwmark and mark matching ─────────────────
CONFIG_NETFILTER_XT_MATCH_CGROUP=y
CONFIG_NETFILTER_XT_TARGET_MARK=y
CONFIG_NETFILTER_XT_MATCH_MARK=y

# ── Policy routing — multiple routing tables + fwmark rules ───────
CONFIG_IP_MULTIPLE_TABLES=y
CONFIG_IP_ROUTE_MULTIPATH=y
CONFIG_IP_ADVANCED_ROUTER=y

# ── WiFi/LTE hardware not present in QEMU — omit BSP-specific configs ──
# (CONFIG_MAC80211, CONFIG_CFG80211, USB serial/QMI are only required
#  on the rpi3/production target, not needed for daemon smoke testing)
